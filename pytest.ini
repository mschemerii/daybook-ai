[pytest]
testpaths = tests
markers =
    ui: real-browser Playwright tests
    streamlit: Streamlit AppTest tests
    llm: tests that send a real request to the local model
    shutdown: test that intentionally stops Daybook AI
addopts = -ra
